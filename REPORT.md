# Отчёт по решению — КосмоХакатон 2026, мониторинг гидрологической обстановки

Решение команды по кейсу «Мониторинг гидрологической активности по данным ДЗЗ».
Стек: SAR-first пиксельный классификатор LightGBM + физическая постобработка, обёрнутый
в FastAPI-сервис с веб-визуализацией.

---

## 1. Задача и протокол оценки

**Что на входе.** Для каждой из 11 пар (район интереса AOI + пара дат «до» / «пик»):

| Модальность | Продукт | Каналы | Разрешение |
|---|---|---|---|
| Sentinel-1 GRD (SAR) | `S1_<pre\|peak>_<date>.tif` | `VV`, `VH`, `VV_VH_ratio` (дБ) | 10 м |
| Sentinel-2 L2A (оптика) | `SENTINEL2_<pre\|peak>_<date>.tif` | `B3,B4,B8,B11` + `NDWI,MNDWI,NDVI,AWEISH` | 10 м |
| AUX (статические) | `AUX_terrain_gsw.tif` | `slope, hand, occurrence, seasonality, max_extent, builtup` | 30 м → варп на 10 м |
| Гидросеть | `vectors/hydrography_osm.geojson` | линейная геометрия рек | — |

Сетка каноническая: эталонная маска `reference_<pair_id>.tif` задаёт проекцию
`EPSG:32652` и шаг 10 м, на неё выравниваются все слои.

**Что на выходе.** `submission.csv` (`pair_id, flood_ha, water_pre_ha, water_peak_ha`,
точность 0.01 га) и `predictions/<pair_id>_flood.tif` (uint8 0/1 на канонической сетке).

**Данные.** 11 пар: **8 событийных** (паводки 2019/2021) + **3 контрольных** (межень
сентября 2018). 5 районов: Благовещенск, Константиновка, Свободный, Белогорск, Поярково.

**Метрика.**
```
Score = 0.45·Q_flood + 0.25·Q_water_peak + 0.15·Q_water_pre + 0.15·Spec_base

Q_*  = среднее по 8 событийным парам от q = max(0, 1 − |X_сабмита − X_эталона| / max(X_эталона, порог))
       порог: затопление 50 га, вода 200 га
Spec_base = среднее по 3 контрольным парам от (1 − min(1, overshoot / 0.005))
       overshoot = max(0, flood_сабмита − flood_эталона) / площадь_AOI
```
Смысл: точность **площади** (не попиксельный IoU), с жёстким штрафом за «фантомные»
затопления на контрольных парах межени (допуск всего 0.5 % площади района).

---

## 2. EDA и наблюдения по данным

Эталонные площади (из `reference_*.json`, площадь `aoi_ha` берётся из JSON как точная):

| Пара (AOI) | группа | AOI, га | flood, га | flood, % | water_pre, га | water_peak, га | water_peak, % | permanent, га |
|---|---|---|---|---|---|---|---|---|
| Благовещенск, межень 2018 | контроль | 164 916.8 | 197.0 | 0.12 | 9 815.2 | 8 469.3 | 5.14 | 7 247.4 |
| Константиновка, межень 2018 | контроль | 123 545.0 | 25.0 | 0.02 | 9 314.3 | 1 730.9 | 1.40 | 5 435.7 |
| Свободный, межень 2018 | контроль | 132 896.5 | 44.2 | 0.03 | 4 626.1 | 3 111.7 | 2.34 | 3 871.6 |
| Белогорск, паводок 2019 | событие | 91 385.1 | 187.2 | 0.20 | 694.3 | 869.3 | 0.95 | 406.9 |
| Благовещенск, паводок 2019 | событие | 164 916.8 | 386.3 | 0.23 | 2 866.9 | 8 894.4 | 5.39 | 7 247.4 |
| Константиновка, паводок 2019 | событие | 123 545.0 | 643.5 | 0.52 | 6 774.1 | 7 267.0 | 5.88 | 5 435.7 |
| Свободный, паводок 2019 | событие | 132 896.5 | 213.3 | 0.16 | 575.2 | 991.5 | 0.75 | 3 871.6 |
| Благовещенск, паводок 06.2021 | событие | 164 916.8 | 882.6 | 0.54 | 2 649.2 | 2 845.2 | 1.73 | 7 247.4 |
| Константиновка, паводок 06.2021 | событие | 123 545.0 | 107.3 | 0.09 | 29.7 | 172.9 | 0.14 | 5 435.7 |
| Поярково, паводок 06.2021 | событие | 109 070.6 | 2 167.3 | 1.99 | 8 723.3 | 8 910.0 | 8.17 | 4 804.7 |
| Свободный, наводнение Зеи 08.2021 | событие | 129 790.0 | 2 179.0 | 1.68 | 3 972.4 | 4 740.9 | 3.65 | 3 596.5 |

**Наблюдения:**

1. **Затопление — крайне редкий класс.** На событийных парах доля flood в среднем
   ≈ 0.68 % площади AOI, разброс 0.09–1.99 % (107–2 179 га). Вода же (water_peak) в
   среднем ≈ 3.3 % AOI. Отсюда — сильный дисбаланс классов и необходимость сэмплирования
   (см. §4).
2. **Постоянное зеркало доминирует в маске воды.** permanent (occurrence ≥ 80 %)
   составляет 2.8–4.4 % AOI — большую часть «воды». Новое затопление — малая дельта
   поверх постоянного русла, поэтому отделение permanent от нового затопления критично.
3. **Сильная межрайонная и межсобытийная вариативность.** Большие события — Поярково
   (2 170 га, 1.99 %) и наводнение на Зее у Свободного (2 179 га, 1.68 %); малые —
   Константиновка 06.2021 (107 га, 0.09 %). Событие 06.2021 в Константиновке почти без
   воды (water_peak 173 га при постоянном русле 5 436 га) — интересный случай «спада
   воды на пике».
4. **Контрольные пары — сценарий спада воды, а не статики.** На всех трёх контрольных
   парах `water_pre` (июль) **больше** `water_peak` (сентябрь): прирост воды отрицательный
   (−13.7 %, −81.4 %, −32.7 %). Это значит, что признак «пик = больше воды» не выполняется
   автоматически — модель обязана отличать затопление от убыли воды (см. физический
   фильтр падения VV в §4/§6).
5. **Оптика есть не у всех: 6 из 11 пар с Sentinel-2, 5 без (≈ 45 %).** Оптики нет у
   **всех трёх контрольных** пар и у двух пар паводка 2019 (Благовещенск, Константиновка).
   Следовательно, SAR — обязательный ядро-канал, оптика — лишь уточнение.
6. **Разрыв между радиолокационной и оптической съёмкой 2–5 суток.** Например, Белогорск
   2019: SAR pre 13.06 / S2 pre 18.06 (5 дн.), SAR peak 25.07 / S2 peak 30.07 (5 дн.).
   Для быстро меняющегося паводка это несовпадение фаз — причина использовать оптику как
   «мягкое» дополнение, а не как равноправный канал.
7. **Характер распределения σ⁰.** VV/VH в дБ; вода даёт низкое обратное рассеяние
   (зеркальное отражение), суша — на 8–15 дБ выше. Ориентиры из нашего физического
   baseline: вода выделяется по VV в диапазоне ≈ −22…−12 дБ (порог Оцу по этому
   диапазону), а новое затопление сопровождается **падением VV ≥ 3 дБ** на пике. Канал
   `VV_VH_ratio` (дБ, разность VV−VH) дополнительно отделяет открытую воду (малый разброс
   поляризаций) от зашумлённой растительностью суши.

---

## 3. Оценка существующих подходов и их ограничений

**Классические пороговые методы** (Otsu по VV, индексные пороги NDWI/MNDWI/AWEISH,
фильтры HAND/уклон). Плюсы — быстрые, интерпретируемые, не требуют разметки. Минусы —
пороги глобальны на сцену и не адаптируются к локальному контексту; чувствительны к
спекл-шуму, углу падения, редкой растительности; «плывут» между сценами/районами и
требуют ручной подстройки. В нашем решении такой рецепт воспроизведён как нижняя граница
(`segmentation/baseline.py`) и как источник физических фильтров.

**Глубокие сети (U-Net/Swin, попиксельная сегментация).** Плюсы — сильная
пространственная модель, учёт контекста. Минусы при наших данных — всего 11 пар / 5 районов
с автоматически построенным эталоном: высокий риск переобучения к конкретным AOI
(не обобщается на «чужие» районы, что прямо штрафует leave-one-AOI-out CV), дорогой
инференс на полных 10-метровых сетках (миллионы пикселей), сложность с гетерогенным
входом (оптика есть не всегда).

**Пиксельные бустинги (LightGBM/XGBoost).** Плюсы — нативно работают с пропусками
(оптика отсутствует у 45 % пар), быстро обучаются и инференсят на миллионах пикселей,
хорошо используют сильные «физические» признаки (HAND, occurrence, slope) вместе с SAR.
Минусы — попиксельная модель без явного пространственного контекста (частично
компенсируется фильтром Ли и морфологической постобработкой). Выбрано как базовое
решение, адекватное объёму данных.

---

## 4. Подход и модель

### Общая схема пайплайна

```
S1/S2/AUX → выравнивание на сетку эталона (варп 30→10 м) → признаки (27) →
LightGBM: P(вода) по окнам pre/peak → порог по F1 → MMU 25 px →
flood = water_peak & ~water_pre & ~permanent & (падение VV ≥ 3 дБ) →
площади → submission.csv + predictions/*_flood.tif
```

**Одна модель на оба окна.** Признак `is_peak` (0 для pre, 1 для peak) сообщает модели
окно, поэтому один классификатор обслуживает и межень, и пик, учась разделять их.

### Объединение радиолокационного и оптического каналов

Схема **SAR-first, оптика как уточнение**:
- **Статические признаки** (AUX + гидроконтекст) — есть всегда.
- **SAR** (`vv, vh, vv_vh_ratio, vv_lee, vh_lee, vv_vh_diff`) — есть всегда (все пары сняты Sentinel-1).
- **Оптика** (`b3,b4,b8,b11` + `ndwi,mndwi,ndvi,aweish`) — только где есть S2.
- Для пар без оптики оптические каналы заполняются **NaN**, а бинарный признак
  `has_optical` сообщает модели о модальности. LightGBM обрабатывает пропуски нативно
  (встроенный сплит по «есть/нет значения»), поэтому одна модель корректно обслуживает
  обе модальности без отдельных веток.

### Признаки (27)

| Группа | Признаки |
|---|---|
| Статические (11) | `slope, hand, occurrence, seasonality, max_extent, builtup, dist_river, hand_le_25, slope_le_5, occ_ge_80, floodable` |
| SAR (6) | `vv, vh, vv_vh_ratio, vv_lee, vh_lee, vv_vh_diff` |
| Оптика (8) | `b3, b4, b8, b11, ndwi, mndwi, ndvi, aweish` |
| Модальность/окно (2) | `has_optical, is_peak` |

Гидроподсказки: `hand_le_25` (HAND ≤ 25 м), `slope_le_5` (уклон ≤ 5°), `occ_ge_80`
(occurrence ≥ 80 %), `floodable = (max_extent > 0.5) | (hand ≤ 25 м и не застройка)`.
`dist_river` — евклидово расстояние до ближайшего пикселя гидросети (м).

### Алгоритм / модель

Пиксельный классификатор **LightGBM** (`objective=binary`): `num_leaves=63`,
`learning_rate=0.05`, `feature_fraction=0.8`, `bagging_fraction=0.8`,
`min_data_in_leaf=50`, `num_boost_round=800`, early-stopping 50. Модель сохраняется как
текстовый `Booster` + JSON с порядком признаков и откалиброванным порогом.

### Процедура обучения

1. Сэмплирование пикселей: **pos_cap = 50 000** положительных (вода) на окно,
   **neg_ratio = 2.0** отрицательных на каждый положительный, seed 42.
2. Внутренний стратифицированный holdout 10 % для early-stopping и калибровки.
3. Порог вероятности подбирается по максимуму **F1** на holdout (перебор по сетке 0.05–0.95).
4. Итоговая модель обучается на всех парах; для сабмита — `predict_all`.

### Схема валидации

Две out-of-группа схемы, отражающие структуру данных:
- **leave-one-event-out** — по событиям (межень 2018, паводок 2019, паводок 06.2021,
  наводнение Зеи 08.2021): проверка временной обобщаемости;
- **leave-one-AOI-out** — по районам (5 фолдов): проверка пространственной обобщаемости
  (ближе всего к тесту на «чужих» районах).

---

## 5. Эксперименты и сравнения

### Проверенные гипотезы

| Гипотеза | Результат |
|---|---|
| SAR-канал — ядро, статические AUX — опора | Подтверждена: без SAR модель вырождается в per-AOI константы (см. ниже) |
| Оптика уточняет, но не обязательна | Частично: 45 % пар без оптики, поэтому SAR-first обязателен |
| Отделение permanent (occurrence ≥ 80 %) от нового затопления | Подтверждена: постоянное русло доминирует в маске воды |
| Фильтр падения VV ≥ 3 дБ убирает «фантомные» затопления на контроле | Подтверждена: без него контрольные пары дают ложные flood |
| MMU 25 px и фильтры HAND/уклон/застройка снимают спекл-выбросы | Подтверждена на визуальном уровне и через Spec_base |

### Ключевой наблюдаемый эффект (деградация при нехватке SAR)

До докачки Sentinel-1 модель обучалась почти только на статических AUX-признаках. Как
следствие, для одного и того же района в **разных событиях** выдавались **одинаковые**
площади (Благовещенск во всех трёх событиях — 2 117.3 / 11 768.2 / 14 057.9 га;
аналогично Константиновка), поскольку статические признаки от события не зависят.
Именно SAR разрывает эту вырожденность: `vv/vh` на pre и peak у события реально разные.
Это прямое обоснование SAR-first подхода.

### Метрики (после переобучения на полном SAR — 14 сцен S1 + 2 сцены S2)

| Схема | Score | Q_flood | Q_water_peak | Q_water_pre | Spec_base |
|---|---|---|---|---|---|
| leave-one-event-out | 0.417 | 0.232 | 0.425 | 0.376 | 0.999 |
| leave-one-AOI-out | 0.442 | 0.250 | 0.507 | 0.358 | 0.996 |
| финал (train) | 0.460 | 0.374 | 0.378 | 0.317 | 0.997 |

Рост относительно run без SAR (0.196 / 0.256 / 0.225) достигнут за счёт двух эффектов:
- **Spec_base** вырос с 0.000 до ≈ 1.0 — физический фильтр падения VV + SAR-каналы
  полностью убрали «фантомные» затопления на контрольных парах (теперь на контроле
  98 / 27 / 28 га при эталоне 197 / 25 / 44 га);
- **Q_water_peak/pre** выросли с ~0.29/0.23 до 0.43–0.51 / 0.36–0.38 — SAR разорвал
  вырожденность к статическим признакам и дал осмысленную динамику воды.

**Остающийся сбой — событие 06.2021 (переобучение к его масштабу).** При валидации с
выносом 06.2021 (единственное событие, где оптика есть у всех трёх районов) модель
завышает воду в разы: flood 31 067 / 25 460 / 11 236 га против эталона 883 / 107 / 2 167 га.
В финальной модели (все пары) та же тенденция: 5 507 / 6 193 / 2 987 га против 883 / 107 /
2 167 га. Это главный источник потерь Q_flood (застревает на ~0.23–0.25). Гипотезы —
перенос масштаба затопления между событиями (в 06.2021 большие площади), а также иной
характер оптических каналов в июне (высокая вегетация) без явной нормализации. См. §8.

---

## 6. Инженерные и предметные решения

- **Спекл-шум:** адаптивный **фильтр Ли** (окно 5, ENL = 5) по каналам VV/VH в дБ —
  сглаживает однородные участки, сохраняет границы; на его выходе считаются `vv_lee`,
  `vh_lee` и физический признак падения VV.
- **Отсутствующая оптика:** пары без S2 получают NaN в оптических каналах + `has_optical=0`;
  LightGBM обрабатывает пропуски нативно, одна модель обслуживает обе модальности.
- **Облачная оптика:** пиксели без валидной оптики трактуются как пропуск (NaN) на уровне
  предобработки S2; детальная пиксельная маска облачности (SCL) — в планах.
- **Локальный угол падения / направление орбиты:** все пары сняты на нисходящей орбите
  (`DESCENDING`, relative orbit 32/105), т.е. геометрия съёмки фактически константна и
  явно не моделируется — это осознанное ограничение текущей версии (см. §8).
- **HAND, уклон и гидросеть:** HAND ≤ 25 м и уклон ≤ 5° как маски «возможного затопления»;
  `dist_river` как признак (зона затопления примыкает к руслу); фильтр застройки
  (`builtup < 0.5`) снимает ложные срабатывания на искусственных покрытиях.
- **Затопленная растительность:** специально не моделируется — отделение открытой воды от
  воды под пологом требует двухполяризационных/поляриметрических признаков и остаётся
  ограничением (см. §8).
- **Ложные срабатывания (радиотени, покрытия):** многоступенчатая фильтрация —
  slope/HAND/builtup-фильтры → `floodable`-подсказка → MMU 25 px → исключение permanent →
  физический фильтр **падение VV ≥ 3 дБ** (радиотень от рельефа, как правило, не даёт
  одновременного падения VV на пике при условии water_peak & ~water_pre).
- **Постобработка контуров:** минимальная картируемая единица **25 px** (8-связные
  компоненты) по постановке; напрямую влияет на итоговую площадь (убирает изолированные
  «озёра» ≤ 0.25 га, типичные спекл-выбросы).

---

## 7. Итоговое решение

В финальную версию вошли:

1. **Пиксельный LightGBM-классификатор** (27 признаков) как единая модель на оба окна.
2. **SAR-first объединение каналов**: SAR всегда, оптика — уточнение через
   NaN + `has_optical`.
3. **Физическая постобработка**: порог по F1 → MMU 25 px →
   `flood = water_peak & ~water_pre & ~permanent & (падение VV ≥ 3 дБ)`;
   `permanent = occurrence ≥ 80 %`.
4. **Гидрологический контекст**: HAND/уклон/застройка как фильтры, `dist_river` и
   производные подсказки как признаки.
5. **Фильтр Ли** для спекла на всех SAR-фичах.
6. **Валидация** leave-one-event-out + leave-one-AOI-out; порог калибруется по F1.

**Почему:** это решение адекватно объёму данных (11 пар), робастно к отсутствующей
оптике, быстро в инференсе на полных сетках и интерпретируемо (каждый физический фильтр
объясним). Deep-модель при таком объёме данных и автоматическом эталоне несёт риск
переобучения к районам.

---

## 8. Выводы и дальнейшее развитие

**Основной вклад отдельных решений:**
- SAR-first + фильтр Ли — устойчивое выделение воды во всепогодном канале;
- отделение permanent через occurrence — без него маска воды «тонет» в постоянном русле;
- физический фильтр падения VV — главный вклад в Spec_base (контроль ложных затоплений);
- гидроконтекст (HAND/уклон/dist_river) — подавление ложных срабатываний на водоразделах
  и покрытиях.

**Типы остающихся ошибок:** пропуск затопленной растительности (вода под пологом);
краевые эффекты варпа 30→10 м и несовпадение фаз SAR/оптики (2–5 дн.); редкие ложные
срабатывания в радиотенях на крутом рельефе.

**Ограничения подхода:** попиксельная модель без явного пространственного контекста;
не моделируется локальный угол падения (геометрия съёмки константна, но при смене орбиты
потребуется нормализация); затопленная растительность не выделяется.

**План улучшений:**
1. Дообучение на полном SAR (14 S1 + 2 S2 уже на диске, остальное докачивается) и
   повторная CV — ожидаемый рост Q_flood и Spec_base.
2. Добавление пространственных признаков (лапласиан/текстура в окне, дистанция в
   поляризационном пространстве) либо лёгкая CNN для контекста.
3. Явная пиксельная маска облачности S2 (SCL) и обработка частичной облачности.
4. Нормализация к локальному углу падения (учёт incidence angle из метаданных S1).
5. Модель/признаки для затопленной растительности (VV/VH-отношение, поляриметрические
   индексы, доп. данные).
6. Калибровка порогов под метрику Score (а не только F1) — порог влияет на площади и,
   следовательно, на сходимость.
